// TODO: Implement WeekSimulationScreen
import 'package:flutter/material.dart';

class WeekSimulationScreen extends StatelessWidget {
  const WeekSimulationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('WeekSimulationScreen — TODO')),
    );
  }
}
