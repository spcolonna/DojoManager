// TODO: Implement TrainingViewModel
import 'package:flutter/material.dart';

class TrainingViewModel extends StatelessWidget {
  const TrainingViewModel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('TrainingViewModel — TODO')),
    );
  }
}
