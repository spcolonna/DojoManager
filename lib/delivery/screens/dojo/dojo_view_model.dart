// TODO: Implement DojoViewModel
import 'package:flutter/material.dart';

class DojoViewModel extends StatelessWidget {
  const DojoViewModel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('DojoViewModel — TODO')),
    );
  }
}
