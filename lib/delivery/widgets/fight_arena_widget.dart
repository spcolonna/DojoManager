// TODO: Implement FightArenaWidget
import 'package:flutter/material.dart';

class FightArenaWidget extends StatelessWidget {
  const FightArenaWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
