// TODO: Implement StrategySliderWidget
import 'package:flutter/material.dart';

class StrategySliderWidget extends StatelessWidget {
  const StrategySliderWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
