// TODO: Implement FightViewModel
import 'package:flutter/material.dart';

class FightViewModel extends StatelessWidget {
  const FightViewModel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('FightViewModel — TODO')),
    );
  }
}
