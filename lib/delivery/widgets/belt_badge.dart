// TODO: Implement BeltBadge
import 'package:flutter/material.dart';

class BeltBadge extends StatelessWidget {
  const BeltBadge({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
