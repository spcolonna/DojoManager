// TODO: Implement DashboardViewModel
import 'package:flutter/material.dart';

class DashboardViewModel extends StatelessWidget {
  const DashboardViewModel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('DashboardViewModel — TODO')),
    );
  }
}
