// TODO: Implement StudentsViewModel
import 'package:flutter/material.dart';

class StudentsViewModel extends StatelessWidget {
  const StudentsViewModel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('StudentsViewModel — TODO')),
    );
  }
}
