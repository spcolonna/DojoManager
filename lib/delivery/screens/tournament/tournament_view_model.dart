// TODO: Implement TournamentViewModel
import 'package:flutter/material.dart';

class TournamentViewModel extends StatelessWidget {
  const TournamentViewModel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('TournamentViewModel — TODO')),
    );
  }
}
