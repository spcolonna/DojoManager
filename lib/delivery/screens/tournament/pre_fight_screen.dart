// TODO: Implement PreFightScreen
import 'package:flutter/material.dart';

class PreFightScreen extends StatelessWidget {
  const PreFightScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('PreFightScreen — TODO')),
    );
  }
}
