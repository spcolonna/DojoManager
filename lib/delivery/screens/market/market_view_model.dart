// TODO: Implement MarketViewModel
import 'package:flutter/material.dart';

class MarketViewModel extends StatelessWidget {
  const MarketViewModel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('MarketViewModel — TODO')),
    );
  }
}
