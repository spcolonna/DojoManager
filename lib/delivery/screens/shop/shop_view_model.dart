// TODO: Implement ShopViewModel
import 'package:flutter/material.dart';

class ShopViewModel extends StatelessWidget {
  const ShopViewModel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('ShopViewModel — TODO')),
    );
  }
}
